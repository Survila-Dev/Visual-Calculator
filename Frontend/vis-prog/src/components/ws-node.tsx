import React from "react"

export const WSNode: React.FC = () => {

    // Click and drag possible, change the coordinates of the node
    // Different state when selected

    return (
        <article>
            This is a node
        </article>
    )
}